# Django Settings
# 환경 변수 예시 파일입니다.
# 실제 사용 시 .env 파일로 복사하고 값을 설정하세요.

# Django 설정 모듈 (선택사항 - 기본값은 development)
# 배포 시에만 설정: DJANGO_SETTINGS_MODULE=anonymous_project.settings.production
# 또는 서버에서: export DJANGO_SETTINGS_MODULE=anonymous_project.settings.production

# SECRET_KEY - 프로덕션에서는 반드시 강력한 키를 사용하세요
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())" 로 생성 가능
SECRET_KEY=your-secret-key-here #SSM Parameter Store

# DEBUG 모드 (True 또는 False)
DEBUG=True # prod -> False

# 허용된 호스트 (쉼표로 구분)
ALLOWED_HOSTS=localhost,127.0.0.1 # prod -> ELB DNS

# 프로덕션 보안 설정 (HTTPS 사용 시)
# SECURE_SSL_REDIRECT=False
# SESSION_COOKIE_SECURE=False
# CSRF_COOKIE_SECURE=False

# 데이터베이스 설정 (PostgreSQL 사용 시)
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432